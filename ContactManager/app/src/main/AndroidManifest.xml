<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions for calling, SMS, and media -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ContactManager">

        <!-- Main entry point -->
        <activity
            android:name=".controller.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".controller.ContactDetailActivity"
            android:exported="false"
            android:parentActivityName=".controller.MainActivity" />

        <activity
            android:name=".controller.AddEditContactActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".controller.GroupsActivity"
            android:exported="false"
            android:parentActivityName=".controller.MainActivity" />

        <activity
            android:name=".controller.GroupDetailActivity"
            android:exported="false"
            android:parentActivityName=".controller.GroupsActivity" />

        <activity
            android:name=".controller.BlacklistActivity"
            android:exported="false"
            android:parentActivityName=".controller.MainActivity" />

        <!--
            SMS BroadcastReceiver for blocking SMS from blacklisted contacts.
            NOTE: On Android 10+ (API 29+), full SMS blocking requires the app to be
            set as the default SMS application. This receiver intercepts SMS broadcasts
            and aborts them for blacklisted numbers at the application level.
        -->
        <receiver
            android:name=".receiver.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for sharing exported CSV files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
